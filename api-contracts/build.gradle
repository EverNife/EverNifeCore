// Compile-time-only "platoverride" stubs: each platform ships the real class under the same
// fully-qualified name, and these are never packaged. Java 8 bytecode so the release-8 modules
// can read them. The stubs are empty and import nothing, so they need no dependencies and no Jabel.
compileJava {
    options.release = 8
}
