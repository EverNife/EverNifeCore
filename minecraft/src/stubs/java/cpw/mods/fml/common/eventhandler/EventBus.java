package cpw.mods.fml.common.eventhandler;

/**
 * Compile-time stand-in for FML's event bus on 1.7.10. The adapters only pass one of these around as
 * an opaque bus handle, so nothing here needs a body - the real class comes from the server.
 */
public class EventBus {
}
