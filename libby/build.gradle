repositories {
    maven { url = 'https://repo.alessiodp.com/releases/' } // the custom libby-core lives here
}

dependencies {
    implementation libs.libby.core
}

// Java 8 bytecode. Plain Java 8 syntax too, so no Jabel and no compiler pin.
compileJava {
    options.release = 8
}
